[project]
name = "lifespan-simulator"
version = "0.1.0"
description = "Interactive mortality and survival simulator for male mortality in Spain."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.2",
    "plotly>=5.24",
    "streamlit>=1.44",
]

